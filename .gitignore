# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.so
*.egg
*.egg-info/
dist/
build/

# Environment
.env
.venv
venv/
ENV/

# Linter / type checker / coverage
.ruff_cache/
.pyright/
.coverage
.coverage.*
htmlcov/
coverage.xml

# Test result reports (published to PRs as CI artifacts, never committed)
reports/

# Security - tokens and credentials
.token
*.pem
*.key
*.crt

# IDE
.vscode/
.idea/
.claude/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Reference material (API specs, course samples, scratch scripts) — kept local, not tracked
docs/


# Per-environment Helm values — hostnames, client ids and cluster specifics that
# belong to a deployment, not to the repo. The chart's own defaults live in
# deploy/helm/sas-mcp-server/values.yaml and are tracked.
deploy/helm/values-*.yaml
